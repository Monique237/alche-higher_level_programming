# Python - Inheritance

This project covers inheritance in Python, including parent classes,
subclasses, method overriding, multiple inheritance, and built-in
functions used with classes and objects.o

